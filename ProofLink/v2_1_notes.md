# v2.1 operator pass
This pass adds:
- a dashboard-first operator view
- a local storefront-to-operator order inbox bridge
- richer order intake fields on the storefront
- owner-facing guidance cards inside the operator
- JSON export for captured order requests

This is still a local-first bridge for order visibility. It is not yet a shared database-backed multi-user order system.
